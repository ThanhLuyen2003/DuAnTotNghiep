import React from "react";
import { View, Image, StyleSheet,Text } from "react-native";


const Home = (props) => {

    return (
        <View style={{flex:1}}>
            <Text></Text>
        </View>
    );
}

export default Home;